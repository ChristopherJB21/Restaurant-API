meta {
  name: Update Username
  type: http
  seq: 6
}

put {
  url: {{base_url}}/user/2
  body: json
  auth: inherit
}

body:json {
  {
    "UserName": "{{$randomFirstName}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
